public class Utility
{
    static void swap(int[] arr, int x, int y)
    {
        int temp = arr[x];
        arr[x] = arr[y];
        arr[y] = temp;
    }


    // Add other repetitive functions here (methods)
    // ...
}